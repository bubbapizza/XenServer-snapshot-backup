#!/bin/sh
#
# This is a config file for setting the defaults for the pro-tools 
# commands.  This is where you configure which database(s) to use and
# what Progress parameter file should be used when connecting to the
# databases.
# 

# Set the directory Progress is installed to.
DLC=@DLC@

#############################
##### PROGRESS DATABASE #####
#############################
#
# By default, the pro-database command will start up Progress databases
# using the default-server.pf parameter file.  To use a different 
# parameter file, name it <database_name>-server.pf.
#
# For example: if you created a parameter file called prodb-server.pf,
# it would override the default-server.pf set of parameters.
#


# Uncomment this to change the path of where the databases are stored.
# DB_PATH=@DB_PATH@

#
# Uncomment this to change the name of the default progress database(s).  
# For more than one database, separate them with a space.
# 
# ####### SQL DATABASES ######
#
# To start a database as a SQL database instead of a 4GL database, 
# add '-SQL' to the end of the database name.  The Progress tools will
# still search for a server parameter file in the format of 
# <database_name>-SQL-server.pf.
#
# For example, a database named "mydb-SQL" will start up the using a
# physical database called "mysql" and will use a parameter file called
# mydb-SQL-server.pf if it exists.
#
# ####### LOGICAL DATABASE NAMES ######
#
# To give a database a logical name different from its physical name,
# use the format <physical-name>/<logical-name>.
#
# For example, a database named "cust-test/customer" will give the 
# physical database "cust-test" a logical name of "customer".
#
# ##################################
# 
# DB_DEFAULT="@DB_DEFAULT@"


###########################
##### PROGRESS CLIENT #####
###########################

# Uncomment this to change the client config file to use for pro-tools
# commands.
# CLIENT_PARAM_FILE="default-client.pf"

# Here's the search path for your Progress source code. 
PROPATH=@PROPATH_DEFAULT@

# This is the shell prompt if you shell out from the Progress IDE.
PS1='$$ '

export PS1 DLC PROPATH 

