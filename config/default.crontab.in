# This is a sample crontab file that you can symlink to under /etc/cron.d
# in order to schedule the backup time.
#

# By default, run full xva backups every Sun. morning at 1:02AM.
2 1 * * 0 root @BINDIR@/xenserver-backup >>@LOGDIR@/xenserver-backup.log 2>>@LOGDIR@/xenserver-backup.err.log
